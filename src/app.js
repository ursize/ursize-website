(function(_0xroot, _0xfactory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = _0xfactory() :
  typeof define === 'function' && define.amd ? define(_0xfactory) :
  (_0xfactory());
})(typeof globalThis !== 'undefined' ? globalThis : typeof self !== 'undefined' ? self : this, function() {
  'use strict';
  const _0x_pool = ["YmctY2FudmFz","MmQ=","I2ZhY2MxNQ==","cmdiYSgyNTUsMjU1LDI1NSwwLjkp","I2ZmZg==","cmdiYSgyNTAsMjA0LDIxLDAuMjIp","cmdiYSgyNTAsMjA0LDIxLDAuMDYp","cmdiYSgyNTAsMjA0LDIxLDAuMDEp","cmdiYSgyNTUsMjU1LDI1NSwwLjIwKQ==","cmdiYSgyNTUsMjU1LDI1NSwwLjA1KQ==","cmdiYSgyNTUsMjU1LDI1NSwwLjAxKQ==","cmdiYSgyNTAsMjA0LDIxLDAuNDUp","cmdiYSgyNTUsMjU1LDI1NSwwLjM4KQ==","I2ZmZmZmZg==","cmdiYSgyNTAsMjA0LDIxLDAuNSk=","cmdiYSgyNTUsMjU1LDI1NSwwLjQ1KQ==","cHg=","cmVzaXpl","bW91c2Vtb3Zl","bW91c2VsZWF2ZQ==","L2NvbmZpZy5qc29u","c2Nyb2xs","Y2xpY2s=","cmdiYSgyNTUsMjU1LDI1NSwwLjcp","cmdiYSgyNTUsMjU1LDI1NSwwLjUp","dHJhbnNwYXJlbnQ=","cmdiYSgwLDAsMCwwLjcyKQ==","cmdiYSgyNTAsMjA0LDIxLDAuMDI4KQ==","cmdiYSgyNTAsMjA0LDIxLDAuMDA4KQ==","cmdiYSg5OSwxMDIsMjQxLDAuMDE4KQ==","cmdiYSgyNTAsMjA0LDIxLDAuMDU1KQ==","cmdiYSgyNTUsMjU1LDI1NSwwLjAxNSk=","IzA3MDcwOQ==","Qm91bmNlIE91dCB4IExpbWVyZW5jZQ==","aHR0cHM6Ly9maWxlcy5jYXRib3gubW9lL2NzMmFyaS5tcDM=","aHR0cHM6Ly9pbWFnZS1jZG4tZmEuc3BvdGlmeWNkbi5jb20vaW1hZ2UvYWI2NzYxNmQwMDAwMWUwMmFmNDEzNDEwMjBjODVmMmYyMjc5YWViMg==","aHR0cHM6Ly9vcGVuLnNwb3RpZnkuY29tL3RyYWNrLzVsWnNoOVFmN0NiSEk5RmNjN1pjc3E=","QWlycGxhbmUgTW9kZQ==","aHR0cHM6Ly9maWxlcy5jYXRib3gubW9lL2g2c3Jtdy5tcDM=","aHR0cHM6Ly9pbWFnZS1jZG4tZmEuc3BvdGlmeWNkbi5jb20vaW1hZ2UvYWI2NzYxNmQwMDAwMWUwMmQ2NTNmODgzNTJjMmRhNDk5MTQ2NDczZg==","aHR0cHM6Ly9vcGVuLnNwb3RpZnkuY29tL3RyYWNrLzVYS1p1YThrcWdId2N4ZUtpTUlqZ2U=","YXV0bw==","YW5vbnltb3Vz","dGltZXVwZGF0ZQ==","dC1jdXI=","dC1kdXI=","dHJhY2stZmlsbA==","ZW5kZWQ=","bG9hZGVkbWV0YWRhdGE=","YWItbm93","YWItZGlzYy1hcnQ=","c3BvdC1saW5r","cC1pYw==","aGlkZGVu","cGEtaWM=","YWItZGlzYw==","c3Bpbg==","ZXEtYm94","YWN0aXZl","dHJrLTA=","dHJrLTE=","Y3RsLXBsYXk=","Y3RsLXByZXY=","Y3RsLW5leHQ=","dm9sLXNsaWRlcg==","aW5wdXQ=","di1pYw==","bS1pYw==","Y3RsLW11dGU=","dHJhY2stc2Vla2Vy","bW91c2Vkb3du","c2NydWJiaW5n","bW91c2V1cA==","dG91Y2hzdGFydA==","dG91Y2htb3Zl","dG91Y2hlbmQ=","MzY3MDI0OTcxNDEyNzk5NDg5","ZGMtbmFtZQ==","ZGMtdGFn","YV8=","Z2lm","cG5n","ZGMtaW1n","ZGMtYmFubmVyLWltZw==","ZGMtZGVj","ZG5k","IzIyYzU1ZQ==","I2Y1OWUwYg==","I2VkNDI0NQ==","IzU4NTg2YQ==","ZGMtZG90","b2ZmbGluZQ==","bm9uZQ==","c3RhdHVzLWRuZA==","LmRwYy1iaW8tYm9keQ==","ZGMtYWN0aXZpdHktYm94","YWN0LWljb24=","YWN0LXR5cGU=","YWN0LW5hbWU=","YWN0LWRldGFpbHM=","YWN0LXN0YXRl","YWN0LWltZw==","ZHBjLWFjdGl2aXR5IGdsYXNzLWNhcmQgYWN0LXNwb3RpZnk=","TElTVEVOSU5HIFRPIFNQT1RJRlk=","MTM=","MCAwIDI0IDI0","IzFkYjk1NA==","","ZHBjLWFjdGl2aXR5IGdsYXNzLWNhcmQgYWN0LWdhbWU=","UExBWUlORyBBIEdBTUU=","U1RSRUFNSU5H","TElTVEVOSU5H","V0FUQ0hJTkc=","QUNUSVZF","IzU4NjVmMg==","cm91bmQ=","NSAzIDE5IDEyIDUgMjEgNSAz","bXA6ZXh0ZXJuYWwv","bXA6ZXh0ZXJuYWwvW14vXSsvaHR0cHMv","aHR0cHM6Ly8=","ZDY5ZWQxYzMxYzAzYjFlOGRjYTJhZjI2MjQzYmE4MjQ=","YV85OGM3NjAwZDMwNGI4NmNhM2IxODI3MmUxZGEwNTU1OQ==","dXJzaXpl","Y3VzdG9t","Q3VzdG9tIFN0YXR1cw==","L3Vyc2l6ZQ==","ZGMtYmFkZ2VzLWRvY2s=","YmFkZ2UtaWNvbg==","JHt4Lm5hbWV9","JHt4Lmljb259","MTk=","d3NzOi8vYXBpLmxhbnlhcmQucmVzdC9zb2NrZXQ=","SU5JVF9TVEFURQ==","UFJFU0VOQ0VfVVBEQVRF","YnRuLWNvcHktaWQ=","dG9hc3Q=","bmwtdHotdGltZQ==","bmwtdHotZGlmZg==","ZW4tR0I=","RXVyb3BlL0Ftc3RlcmRhbQ==","Mi1kaWdpdA==","ZW4tVVM=","KHNhbWUgdGltZSk=","MjAyNy0wMS0wMVQwMDowMDowMFo=","Yy1k","Yy1o","Yy1t","Yy1z","dG9hc3QtdGV4dA==","dGV4dGFyZWE=","cmVhZG9ubHk=","Zml4ZWQ=","LTk5OTlweA==","Y29weQ==","QWRkcmVzcyBjb3BpZWQgdG8gY2xpcGJvYXJk","MCAwICR7d30gJHt3fQ==","aHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmc=","JHt3fQ==","IzBkMGQxMg==","JHtjICogY2VsbH0=","JHtyICogY2VsbH0=","JHtjZWxsfQ==","LmJ0bi1jb3B5LCAuY3AtYnRu","ZGF0YS10aWQ=","ZGF0YS1hZGRy","LmMtYmFy","cXItbW9kYWw=","LmJ0bi1xciwgLnFyLWJ0bg==","ZGF0YS1jb2lu","Q3J5cHRv","bS10aXRsZQ==","bS1hZGRy","bS1xci1zdmc=","bS1jbG9zZQ==","bS1jb3B5","W2RhdGEtdGlsdF0=","cGVyc3BlY3RpdmUoODAwcHgpIHJvdGF0ZVgoMGRlZykgcm90YXRlWSgwZGVnKSB0cmFuc2xhdGVZKDBweCk=","bmF2LXBpbGwtYmFy","bmF2LWJ1YmJsZQ==","LnRs","bW91c2VlbnRlcg==","aHJlZg==","c21vb3Ro","c2VydmljZXM=","cHJvamVjdA==","Y3J5cHRv","YVtocmVmPSIjY3J5cHRvIl0=","YVtocmVmPSIjcHJvamVjdCJd","YVtocmVmPSIjc2VydmljZXMiXQ==","LnRvcC1uYXYtd3JhcA==","c2Nyb2xsZWQ=","LnRsLmFjdGl2ZQ==","c3RhdC1leHA=","c3RhdC1zYWZl","c3RhdC15ZWFy","IHlycw==","LS1zaGluZS14","LS1zaGluZS15","cGVyc3BlY3RpdmUoODAwcHgpIHJvdGF0ZVgoMGRlZykgcm90YXRlWSgwZGVnKSB0cmFuc2xhdGVZKDAp","NTAl","LmdsYXNzLWNhcmQsIC5wcm9qZWN0LWJveCwgLmMtY2FyZCwgLnNlcnZpY2UtY2FyZCwgLnZpZXdzLXBpbGw=","ZGF0YS10aWx0","OmhvdmVy","YnViYmx5LXNob3c=","LnNlYy13cmFwLCAucHJvamVjdC1ib3gsIC5ncmlkLWNyeXB0bywgLnNlcnZpY2VzLWdyaWQ=","YnViYmx5LXJldmVhbA==","dmlldy1jb3VudA==","L2FwaS92aWV3cw==","RE9NQ29udGVudExvYWRlZA==","YnVyZ2VyLWJ0bg==","bW9iLWRyYXdlcg==","b3Blbg==","Lm1vYi1saW5r"];
  function _0x_get(_0xidx) {
    const _0xraw = _0x_pool[_0xidx];
    if (typeof atob !== 'undefined') {
      return decodeURIComponent(escape(atob(_0xraw)));
    }
    return Buffer.from(_0xraw, 'base64').toString('utf8');
  }

function init_canvas() {
  const cvs = document.getElementById(_0x_get(0));
  if (!cvs) return;
  const ctx = cvs.getContext(_0x_get(1));

  let W = 0, H = 0, dpr = 1;
  let ptr = { x: -3000, y: -3000, tx: -3000, ty: -3000, vx: 0, vy: 0 };
  let time = 0;
  let ripples = [];
  let sparks = [];
  let bubbles = [];
  let nodes = [];

  const GRID = 52;

  class Ripple {
    constructor(x, y, col) {
      this.x = x; this.y = y;
      this.r = 0; this.maxR = 380;
      this.alpha = 1; this.speed = 7;
      this.col = col || _0x_get(2);
    }
    step() {
      this.r += this.speed + this.r * 0.018;
      this.alpha -= 0.022;
      return this.alpha > 0 && this.r < this.maxR;
    }
    draw(c) {
      c.save();
      c.beginPath();
      c.arc(this.x, this.y, this.r, 0, Math.PI * 2);
      c.strokeStyle = this.col;
      c.globalAlpha = this.alpha * 0.6;
      c.lineWidth = 1.5;
      c.stroke();
      c.beginPath();
      c.arc(this.x, this.y, Math.max(0, this.r - 18), 0, Math.PI * 2);
      c.strokeStyle = this.col;
      c.globalAlpha = this.alpha * 0.25;
      c.lineWidth = 0.8;
      c.stroke();
      c.restore();
    }
  }

  class Spark {
    constructor(x, y) {
      const ang = Math.random() * Math.PI * 2;
      const spd = 1.2 + Math.random() * 2.8;
      this.x = x; this.y = y;
      this.vx = Math.cos(ang) * spd;
      this.vy = Math.sin(ang) * spd;
      this.alpha = 0.8 + Math.random() * 0.2;
      this.r = 1.5 + Math.random() * 1.8;
      this.decay = 0.025 + Math.random() * 0.02;
      this.gold = Math.random() < 0.6;
    }
    step() {
      this.x += this.vx;
      this.y += this.vy;
      this.vy += 0.04;
      this.vx *= 0.97;
      this.alpha -= this.decay;
      return this.alpha > 0;
    }
    draw(c) {
      c.save();
      c.globalAlpha = this.alpha;
      c.beginPath();
      c.arc(this.x, this.y, this.r, 0, Math.PI * 2);
      c.fillStyle = this.gold ? _0x_get(2) : _0x_get(3);
      c.shadowColor = this.gold ? _0x_get(2) : _0x_get(4);
      c.shadowBlur = 8;
      c.fill();
      c.restore();
    }
  }

  class Bubble {
    constructor(x, y, r, vy) {
      this.x = x;
      this.y = y;
      this.r = r || (6 + Math.random() * 10);
      this.vy = vy || -(0.8 + Math.random() * 1.5);
      this.vx = (Math.random() - 0.5) * 0.45;
      this.wobble = Math.random() * Math.PI * 2;
      this.wobbleSpd = 0.02 + Math.random() * 0.025;
      this.alpha = 0.45 + Math.random() * 0.35;
      this.decay = 0.0035 + Math.random() * 0.004;
      this.gold = Math.random() < 0.28;
    }
    step() {
      this.wobble += this.wobbleSpd;
      this.x += this.vx + Math.sin(this.wobble) * 0.45;
      this.y += this.vy;
      this.alpha -= this.decay;
      return this.alpha > 0 && this.y > -60 && this.y < H + 60;
    }
    draw(c) {
      c.save();
      c.globalAlpha = Math.max(0, this.alpha);
      const bgGrad = c.createRadialGradient(
        this.x - this.r * 0.35, this.y - this.r * 0.35, 1,
        this.x, this.y, this.r
      );
      if (this.gold) {
        bgGrad.addColorStop(0, _0x_get(5));
        bgGrad.addColorStop(0.6, _0x_get(6));
        bgGrad.addColorStop(1, _0x_get(7));
      } else {
        bgGrad.addColorStop(0, _0x_get(8));
        bgGrad.addColorStop(0.6, _0x_get(9));
        bgGrad.addColorStop(1, _0x_get(10));
      }
      c.fillStyle = bgGrad;
      c.beginPath();
      c.arc(this.x, this.y, this.r, 0, Math.PI * 2);
      c.fill();

      c.strokeStyle = this.gold ? _0x_get(11) : _0x_get(12);
      c.lineWidth = 1;
      c.stroke();

      c.beginPath();
      c.arc(this.x - this.r * 0.35, this.y - this.r * 0.35, Math.max(1, this.r * 0.24), 0, Math.PI * 2);
      c.fillStyle = _0x_get(13);
      c.shadowColor = this.gold ? _0x_get(2) : _0x_get(13);
      c.shadowBlur = 6;
      c.fill();

      c.beginPath();
      c.arc(this.x + this.r * 0.32, this.y + this.r * 0.32, Math.max(0.8, this.r * 0.16), 0, Math.PI * 2);
      c.fillStyle = this.gold ? _0x_get(14) : _0x_get(15);
      c.fill();
      c.restore();
    }
  }


  function rsz() {
    dpr = Math.min(window.devicePixelRatio || 1, 2);
    W = window.innerWidth;
    H = window.innerHeight;
    cvs.width = Math.floor(W * dpr);
    cvs.height = Math.floor(H * dpr);
    cvs.style.width = W + _0x_get(16);
    cvs.style.height = H + _0x_get(16);
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);

    nodes = [];
    const cols = Math.ceil(W / GRID) + 1;
    const rows = Math.ceil(H / GRID) + 1;
    for (let r = 0; r <= rows; r++) {
      for (let c = 0; c <= cols; c++) {
        nodes.push({
          bx: c * GRID,
          by: r * GRID,
          ox: 0, oy: 0,
          glow: 0,
          phase: Math.random() * Math.PI * 2,
          speed: 0.004 + Math.random() * 0.004
        });
      }
    }
  }

  window.addEventListener(_0x_get(17), rsz);
  rsz();

  window.addEventListener(_0x_get(18), e => {
    ptr.vx = e.clientX - ptr.tx;
    ptr.vy = e.clientY - ptr.ty;
    ptr.tx = e.clientX;
    ptr.ty = e.clientY;
  });

  window.addEventListener(_0x_get(19), () => {
    ptr.tx = -3000; ptr.ty = -3000;
  });

  let allowBubbles = true;
  fetch(_0x_get(20)).then(r => r.json()).then(c => {
    if (c?.bubbles === false) {
      allowBubbles = false;
      bubbles.length = 0;
    }
  }).catch(() => {});

  function spawnBubble(dir, speedFactor) {
    if (!allowBubbles) return;
    let bx;
    const side = Math.random();
    if (side < 0.45) bx = W * (0.02 + Math.random() * 0.16);
    else if (side < 0.90) bx = W * (0.82 + Math.random() * 0.16);
    else bx = W * (0.28 + Math.random() * 0.44);
    const by = dir > 0 ? H + 20 : -20;
    const vy = dir > 0 ? -(1.2 + Math.random() * 2.2) * speedFactor : (1.2 + Math.random() * 2.2) * speedFactor;
    const r = (Math.random() < 0.25 ? 12 + Math.random() * 8 : 5 + Math.random() * 7);
    bubbles.push(new Bubble(bx, by, r, vy));
  }

  for (let i = 0; i < 12; i++) {
    let bx;
    const side = Math.random();
    if (side < 0.45) bx = W * (0.03 + Math.random() * 0.16);
    else if (side < 0.90) bx = W * (0.81 + Math.random() * 0.16);
    else bx = W * (0.25 + Math.random() * 0.5);
    bubbles.push(new Bubble(bx, Math.random() * H, 5 + Math.random() * 9, -(0.4 + Math.random() * 0.8)));
  }

  let lastSY = window.scrollY;
  window.addEventListener(_0x_get(21), () => {
    if (!allowBubbles) return;
    const sy = window.scrollY;
    const dy = sy - lastSY;
    lastSY = sy;
    const count = Math.min(3, Math.floor(Math.abs(dy) * 0.15));
    for (let i = 0; i < count; i++) {
      spawnBubble(dy >= 0 ? 1 : -1, Math.min(1.8, 1 + Math.abs(dy) * 0.02));
    }
  }, { passive: true });


  window.addEventListener(_0x_get(22), e => {
    ripples.push(new Ripple(e.clientX, e.clientY, _0x_get(2)));
    setTimeout(() => ripples.push(new Ripple(e.clientX, e.clientY, _0x_get(23))), 80);
    for (let i = 0; i < 14; i++) sparks.push(new Spark(e.clientX, e.clientY));
  });

  setTimeout(() => {
    ripples.push(new Ripple(W * 0.38, H * 0.40, _0x_get(2)));
  }, 180);
  setTimeout(() => {
    ripples.push(new Ripple(W * 0.38, H * 0.40, _0x_get(24)));
    for (let i = 0; i < 8; i++) sparks.push(new Spark(W * 0.38, H * 0.40));
  }, 520);

  function draw_vignette() {
    const g = ctx.createRadialGradient(W * 0.5, H * 0.42, H * 0.1, W * 0.5, H * 0.42, H * 0.85);
    g.addColorStop(0, _0x_get(25));
    g.addColorStop(1, _0x_get(26));
    ctx.fillStyle = g;
    ctx.fillRect(0, 0, W, H);
  }

  function draw_aurora() {
    const ax = W * 0.5 + Math.cos(time * 0.18) * W * 0.22;
    const ay = H * 0.3 + Math.sin(time * 0.22) * H * 0.1;
    const g1 = ctx.createRadialGradient(ax, ay, 0, ax, ay, W * 0.55);
    g1.addColorStop(0, _0x_get(27));
    g1.addColorStop(0.5, _0x_get(28));
    g1.addColorStop(1, _0x_get(25));
    ctx.fillStyle = g1;
    ctx.fillRect(0, 0, W, H);

    const bx = W * 0.72 + Math.cos(time * 0.14 + 2) * W * 0.12;
    const by = H * 0.6 + Math.sin(time * 0.17 + 1) * H * 0.12;
    const g2 = ctx.createRadialGradient(bx, by, 0, bx, by, W * 0.35);
    g2.addColorStop(0, _0x_get(29));
    g2.addColorStop(1, _0x_get(25));
    ctx.fillStyle = g2;
    ctx.fillRect(0, 0, W, H);
  }

  function draw_grid_and_nodes() {
    const proxR = 300;
    const tiltStr = 5;

    ctx.lineWidth = 0.5;

    for (let n of nodes) {
      const dx = n.bx - ptr.x;
      const dy = n.by - ptr.y;
      const dist = Math.sqrt(dx * dx + dy * dy);
      const prox = dist < proxR ? (1 - dist / proxR) : 0;

      const ripPush = (() => {
        let rx = 0, ry = 0;
        for (let rp of ripples) {
          const rdx = n.bx - rp.x;
          const rdy = n.by - rp.y;
          const rdist = Math.sqrt(rdx * rdx + rdy * rdy);
          const diff = Math.abs(rdist - rp.r);
          if (diff < 55) {
            const factor = (1 - diff / 55) * rp.alpha * 10;
            rx += (rdx / Math.max(1, rdist)) * factor;
            ry += (rdy / Math.max(1, rdist)) * factor;
          }
        }
        return { x: rx, y: ry };
      })();

      n.ox += ((-dx / Math.max(1, dist)) * prox * tiltStr - n.ox) * 0.12;
      n.oy += ((-dy / Math.max(1, dist)) * prox * tiltStr - n.oy) * 0.12;
      n.glow += (prox - n.glow) * 0.1;

      n.nx = n.bx + n.ox + ripPush.x;
      n.ny = n.by + n.oy + ripPush.y;

      const wave = Math.sin(n.phase + time * n.speed * 300) * 0.5 + 0.5;
      n.baseAlpha = 0.04 + wave * 0.025;
    }

    const cols_count = Math.ceil(W / GRID) + 2;
    const rows_count = Math.ceil(H / GRID) + 2;

    for (let r = 0; r < rows_count - 1; r++) {
      for (let c = 0; c < cols_count - 1; c++) {
        const i = r * cols_count + c;
        const right = i + 1;
        const down = i + cols_count;
        const n = nodes[i];
        const nr = nodes[right];
        const nd = nodes[down];
        if (!n || !nr || !nd) continue;

        const avgGlow = (n.glow + nr.glow) * 0.5;
        ctx.beginPath();
        ctx.moveTo(n.nx, n.ny);
        ctx.lineTo(nr.nx, nr.ny);
        ctx.strokeStyle = avgGlow > 0.02
          ? `rgba(250,204,21,${0.04 + avgGlow * 0.14})`
          : `rgba(255,255,255,${n.baseAlpha})`;
        ctx.stroke();

        const avgGlowV = (n.glow + nd.glow) * 0.5;
        ctx.beginPath();
        ctx.moveTo(n.nx, n.ny);
        ctx.lineTo(nd.nx, nd.ny);
        ctx.strokeStyle = avgGlowV > 0.02
          ? `rgba(250,204,21,${0.04 + avgGlowV * 0.14})`
          : `rgba(255,255,255,${n.baseAlpha})`;
        ctx.stroke();
      }
    }

    const arm = 4;
    for (let n of nodes) {
      if (n.glow > 0.04) {
        ctx.save();
        ctx.strokeStyle = `rgba(250,204,21,${0.18 + n.glow * 0.7})`;
        ctx.lineWidth = 1.2;
        ctx.shadowColor = _0x_get(2);
        ctx.shadowBlur = 6 + n.glow * 14;
        ctx.beginPath();
        ctx.moveTo(n.nx - arm, n.ny); ctx.lineTo(n.nx + arm, n.ny);
        ctx.moveTo(n.nx, n.ny - arm); ctx.lineTo(n.nx, n.ny + arm);
        ctx.stroke();
        ctx.restore();
      } else {
        ctx.fillStyle = `rgba(255,255,255,${n.baseAlpha + 0.04})`;
        ctx.fillRect(n.nx - 0.5, n.ny - 0.5, 1.5, 1.5);
      }
    }
  }

  function draw_cursor_light() {
    if (ptr.x < -500) return;
    const g = ctx.createRadialGradient(ptr.x, ptr.y, 0, ptr.x, ptr.y, 260);
    g.addColorStop(0, _0x_get(30));
    g.addColorStop(0.35, _0x_get(31));
    g.addColorStop(1, _0x_get(25));
    ctx.fillStyle = g;
    ctx.fillRect(0, 0, W, H);

    const speed = Math.sqrt(ptr.vx * ptr.vx + ptr.vy * ptr.vy);
    if (speed > 3) {
      const trailG = ctx.createRadialGradient(ptr.x, ptr.y, 0, ptr.x, ptr.y, 90);
      trailG.addColorStop(0, `rgba(250,204,21,${Math.min(0.12, speed * 0.008)})`);
      trailG.addColorStop(1, _0x_get(25));
      ctx.fillStyle = trailG;
      ctx.fillRect(0, 0, W, H);
    }
  }

  function ploop() {
    time += 0.016;
    ptr.x += (ptr.tx - ptr.x) * 0.1;
    ptr.y += (ptr.ty - ptr.y) * 0.1;

    ctx.fillStyle = _0x_get(32);
    ctx.fillRect(0, 0, W, H);

    draw_aurora();

    for (let i = ripples.length - 1; i >= 0; i--) {
      if (!ripples[i].step()) ripples.splice(i, 1);
      else ripples[i].draw(ctx);
    }

    draw_grid_and_nodes();
    draw_cursor_light();
    draw_vignette();

    for (let i = sparks.length - 1; i >= 0; i--) {
      if (!sparks[i].step()) sparks.splice(i, 1);
      else sparks[i].draw(ctx);
    }

    for (let i = bubbles.length - 1; i >= 0; i--) {
      if (!bubbles[i].step()) bubbles.splice(i, 1);
      else bubbles[i].draw(ctx);
    }


    requestAnimationFrame(ploop);
  }
  ploop();
}


function init_audio() {
  const fkk = x => document.getElementById(x);
  
  let fuckingfuckingor = false;
  let shiisisisisiis = 0;
  let damnn = 0.7;
  let wtf = false;
  let cur = 0;
  let is_scrubbing = false;

  const trklist = [
    {
      n: _0x_get(33),
      len: 165,
      src: _0x_get(34),
      art: _0x_get(35),
      spot: _0x_get(36)
    },
    {
      n: _0x_get(37),
      len: 142,
      src: _0x_get(38),
      art: _0x_get(39),
      spot: _0x_get(40)
    }
  ];

  let real_aud = new Audio();
  real_aud.preload = _0x_get(41);
  real_aud.crossOrigin = _0x_get(42);
  real_aud.src = trklist[0].src;
  real_aud.volume = damnn;

  real_aud.addEventListener(_0x_get(43), () => {
    if (is_scrubbing) return;
    cur = real_aud.currentTime;
    const dur = real_aud.duration || trklist[shiisisisisiis].len;
    const m = Math.floor(cur / 60);
    const s = Math.floor(cur % 60).toString().padStart(2, '0');
    fkk(_0x_get(44)).textContent = `${m}:${s}`;
    const dm = Math.floor(dur / 60);
    const ds = Math.floor(dur % 60).toString().padStart(2, '0');
    fkk(_0x_get(45)).textContent = `${dm}:${ds}`;
    fkk(_0x_get(46)).style.width = `${(cur / dur) * 100}%`;
  });

  real_aud.addEventListener(_0x_get(47), () => {
    switch_trk((shiisisisisiis + 1) % trklist.length);
    real_aud.play().catch(() => {});
  });

  real_aud.addEventListener(_0x_get(48), () => {
    const dur = real_aud.duration || trklist[shiisisisisiis].len;
    const dm = Math.floor(dur / 60);
    const ds = Math.floor(dur % 60).toString().padStart(2, '0');
    fkk(_0x_get(45)).textContent = `${dm}:${ds}`;
  });

  function up_aud_dom() {
    const trk = trklist[shiisisisisiis];
    fkk(_0x_get(49)).textContent = trk.n;
    const art_img = fkk(_0x_get(50));
    if (art_img && art_img.src !== trk.art) art_img.src = trk.art;
    const spt = fkk(_0x_get(51));
    if (spt) spt.href = trk.spot;
  }

  function play_flip() {
    fuckingfuckingor = !fuckingfuckingor;

    if (fuckingfuckingor) {
      real_aud.play().then(() => {
        fkk(_0x_get(52)).classList.add(_0x_get(53));
        fkk(_0x_get(54)).classList.remove(_0x_get(53));
        fkk(_0x_get(55)).classList.add(_0x_get(56));
        fkk(_0x_get(57)).classList.add(_0x_get(58));
      }).catch(() => {
        fuckingfuckingor = false;
        fkk(_0x_get(52)).classList.remove(_0x_get(53));
        fkk(_0x_get(54)).classList.add(_0x_get(53));
        fkk(_0x_get(55)).classList.remove(_0x_get(56));
        fkk(_0x_get(57)).classList.remove(_0x_get(58));
      });
    } else {
      real_aud.pause();
      fkk(_0x_get(52)).classList.remove(_0x_get(53));
      fkk(_0x_get(54)).classList.add(_0x_get(53));
      fkk(_0x_get(55)).classList.remove(_0x_get(56));
      fkk(_0x_get(57)).classList.remove(_0x_get(58));
    }
  }

  function switch_trk(idx) {
    shiisisisisiis = idx;
    real_aud.src = trklist[shiisisisisiis].src;
    real_aud.currentTime = 0;
    fkk(_0x_get(59)).classList.toggle(_0x_get(58), idx === 0);
    fkk(_0x_get(60)).classList.toggle(_0x_get(58), idx === 1);
    up_aud_dom();
    if (fuckingfuckingor) {
      real_aud.play().catch(() => {});
    }
  }

  fkk(_0x_get(61)).addEventListener(_0x_get(22), play_flip);
  fkk(_0x_get(62)).addEventListener(_0x_get(22), () => {
    switch_trk((shiisisisisiis - 1 + trklist.length) % trklist.length);
  });
  fkk(_0x_get(63)).addEventListener(_0x_get(22), () => {
    switch_trk((shiisisisisiis + 1) % trklist.length);
  });
  fkk(_0x_get(59)).addEventListener(_0x_get(22), () => switch_trk(0));
  fkk(_0x_get(60)).addEventListener(_0x_get(22), () => switch_trk(1));

  fkk(_0x_get(64)).addEventListener(_0x_get(65), e => {
    damnn = parseFloat(e.target.value) / 100;
    real_aud.volume = damnn;
    if (damnn === 0) {
      wtf = true;
      fkk(_0x_get(66)).classList.add(_0x_get(53));
      fkk(_0x_get(67)).classList.remove(_0x_get(53));
    } else {
      wtf = false;
      fkk(_0x_get(66)).classList.remove(_0x_get(53));
      fkk(_0x_get(67)).classList.add(_0x_get(53));
    }
  });

  fkk(_0x_get(68)).addEventListener(_0x_get(22), () => {
    wtf = !wtf;
    real_aud.muted = wtf;
    fkk(_0x_get(66)).classList.toggle(_0x_get(53), wtf);
    fkk(_0x_get(67)).classList.toggle(_0x_get(53), !wtf);
  });

  const seeker_el = fkk(_0x_get(69));

  function do_scrub(cx) {
    const rc = seeker_el.getBoundingClientRect();
    const p = Math.max(0, Math.min(1, (cx - rc.left) / rc.width));
    const dur = real_aud.duration || trklist[shiisisisisiis].len;
    const tgt_t = p * dur;

    fkk(_0x_get(46)).style.width = `${p * 100}%`;
    const m = Math.floor(tgt_t / 60);
    const s = Math.floor(tgt_t % 60).toString().padStart(2, '0');
    fkk(_0x_get(44)).textContent = `${m}:${s}`;
    return tgt_t;
  }

  seeker_el.addEventListener(_0x_get(70), e => {
    is_scrubbing = true;
    seeker_el.classList.add(_0x_get(71));
    const t = do_scrub(e.clientX);
    real_aud.currentTime = t;
  });

  window.addEventListener(_0x_get(18), e => {
    if (!is_scrubbing) return;
    const t = do_scrub(e.clientX);
    real_aud.currentTime = t;
  });

  window.addEventListener(_0x_get(72), e => {
    if (!is_scrubbing) return;
    is_scrubbing = false;
    seeker_el.classList.remove(_0x_get(71));
    const t = do_scrub(e.clientX);
    real_aud.currentTime = t;
  });

  seeker_el.addEventListener(_0x_get(73), e => {
    if (e.touches && e.touches[0]) {
      is_scrubbing = true;
      seeker_el.classList.add(_0x_get(71));
      const t = do_scrub(e.touches[0].clientX);
      real_aud.currentTime = t;
    }
  }, { passive: true });

  window.addEventListener(_0x_get(74), e => {
    if (!is_scrubbing || !e.touches || !e.touches[0]) return;
    const t = do_scrub(e.touches[0].clientX);
    real_aud.currentTime = t;
  }, { passive: true });

  window.addEventListener(_0x_get(75), e => {
    if (!is_scrubbing) return;
    is_scrubbing = false;
    seeker_el.classList.remove(_0x_get(71));
  });

  return { play_flip };
}


function init_discord() {
  const fkk = x => document.getElementById(x);
  const DUID = _0x_get(76);

  function fill_dc(d) {
    if (!d) return;
    const u = d.discord_user;
    if (u) {
      fkk(_0x_get(77)).textContent = u.global_name || u.display_name || u.username;
      fkk(_0x_get(78)).textContent = `@${u.username}`;
      if (u.avatar) {
        const ext = u.avatar.startsWith(_0x_get(79)) ? _0x_get(80) : _0x_get(81);
        fkk(_0x_get(82)).src = `https://cdn.discordapp.com/avatars/${u.id}/${u.avatar}.${ext}?size=256`;
      }
      if (u.banner) {
        const b_ext = u.banner.startsWith(_0x_get(79)) ? _0x_get(80) : _0x_get(81);
        const b_el = fkk(_0x_get(83));
        if (b_el) b_el.src = `https://cdn.discordapp.com/banners/${u.id}/${u.banner}.${b_ext}?size=600`;
      }
      if (u.avatar_decoration_data && u.avatar_decoration_data.asset) {
        const dec = fkk(_0x_get(84));
        if (dec) {
          dec.src = `https://cdn.discordapp.com/avatar-decoration-presets/${u.avatar_decoration_data.asset}.png?size=160`;
          dec.classList.remove(_0x_get(53));
        }
      }
    }

    const s = d.discord_status || _0x_get(85);
    const cdict = {
      online: _0x_get(86),
      idle: _0x_get(87),
      dnd: _0x_get(88),
      offline: _0x_get(89)
    };
    const dot = fkk(_0x_get(90));
    dot.style.background = cdict[s] || _0x_get(88);
    dot.style.boxShadow = s !== _0x_get(91) ? `0 0 10px ${cdict[s]}` : _0x_get(92);
    if (s === _0x_get(85)) dot.classList.add(_0x_get(93));
    else dot.classList.remove(_0x_get(93));

    const bio = document.querySelector(_0x_get(94));
    if (d.activities && d.activities.length) {
      const custom = d.activities.find(x => x.type === 4);
      if (custom && custom.state && bio) {
        bio.textContent = custom.state;
      }
    }

    const act_box = fkk(_0x_get(95));
    const act_icon = fkk(_0x_get(96));
    const act_type = fkk(_0x_get(97));
    const act_name = fkk(_0x_get(98));
    const act_det = fkk(_0x_get(99));
    const act_st = fkk(_0x_get(100));
    const act_img = fkk(_0x_get(101));

    if (act_box) {
      if (d.listening_to_spotify && d.spotify) {
        act_box.classList.remove(_0x_get(53));
        act_box.className = _0x_get(102);
        act_type.textContent = _0x_get(103);
        act_icon.innerHTML = `<svg width=_0x_get(104) height=_0x_get(104) viewBox=_0x_get(105) fill=_0x_get(106)><path d=_0x_get(107)/></svg>`;
        act_name.textContent = d.spotify.song;
        act_det.textContent = `by ${d.spotify.artist}`;
        act_st.textContent = d.spotify.album || '';
        if (d.spotify.album_art_url) {
          act_img.src = d.spotify.album_art_url;
          act_img.classList.remove(_0x_get(53));
        } else {
          act_img.classList.add(_0x_get(53));
        }
      } else {
        const rich = (d.activities || []).find(x => x.type !== 4);
        if (rich) {
          act_box.classList.remove(_0x_get(53));
          act_box.className = _0x_get(108);
          if (rich.type === 0) act_type.textContent = _0x_get(109);
          else if (rich.type === 1) act_type.textContent = _0x_get(110);
          else if (rich.type === 2) act_type.textContent = _0x_get(111);
          else if (rich.type === 3) act_type.textContent = _0x_get(112);
          else act_type.textContent = _0x_get(113);

          act_icon.innerHTML = `<svg width=_0x_get(104) height=_0x_get(104) viewBox=_0x_get(105) fill=_0x_get(92) stroke=_0x_get(114) stroke-width="2" stroke-linecap=_0x_get(115) stroke-linejoin=_0x_get(115)><polygon points=_0x_get(116)/></svg>`;
          act_name.textContent = rich.name;
          act_det.textContent = rich.details || '';
          act_st.textContent = rich.state || '';

          if (rich.assets && rich.assets.large_image) {
            if (rich.assets.large_image.startsWith(_0x_get(117))) {
              const extUrl = rich.assets.large_image.replace(new RegExp(_0x_get(118)), _0x_get(119));
              act_img.src = extUrl;
            } else {
              act_img.src = `https://cdn.discordapp.com/app-assets/${rich.application_id}/${rich.assets.large_image}.png`;
            }
            act_img.classList.remove(_0x_get(53));
          } else {
            act_img.classList.add(_0x_get(53));
          }
        } else {
          act_box.classList.add(_0x_get(53));
        }
      }
    }
  }

  const init_dc_data = {
    discord_user: {
      avatar: _0x_get(120),
      avatar_decoration_data: { asset: _0x_get(121) },
      id: DUID,
      username: _0x_get(122),
      global_name: _0x_get(122)
    },
    activities: [
      { id: _0x_get(123), name: _0x_get(124), state: _0x_get(125), type: 4 }
    ],
    discord_status: _0x_get(85)
  };
  fill_dc(init_dc_data);

  fetch(_0x_get(20)).then(r=>r.json()).then(c=>{
    const b = c?.profile?.badges;
    const dk = fkk(_0x_get(126));
    if (b && dk) {
      dk.innerHTML = b.map(x=>`<span class=_0x_get(127) title=_0x_get(128)><img src=_0x_get(129) alt=_0x_get(128) width=_0x_get(130) height=_0x_get(130)></span>`).join('');
    }
  }).catch(()=>{});

  function dc_hook() {
    const ws = new WebSocket(_0x_get(131));
    let hb = null;

    ws.onopen = () => {
      ws.send(JSON.stringify({ op: 2, d: { subscribe_to_id: DUID } }));
    };

    ws.onmessage = ev => {
      try {
        const d = JSON.parse(ev.data);
        if (d.op === 1) {
          hb = setInterval(() => ws.send(JSON.stringify({ op: 3 })), d.d.heartbeat_interval);
        }
        if (d.t === _0x_get(132) || d.t === _0x_get(133)) {
          fill_dc(d.d);
        }
      } catch (_) {}
    };

    ws.onerror = () => {
      fetch(`https://api.lanyard.rest/v1/users/${DUID}`)
        .then(r => r.json())
        .then(r => { if (r.data) fill_dc(r.data); })
        .catch(() => {});
    };

    ws.onclose = () => {
      if (hb) clearInterval(hb);
      setTimeout(dc_hook, 10000);
    };
  }
  dc_hook();

  const cid_btn = fkk(_0x_get(134));
  if (cid_btn) {
    cid_btn.addEventListener(_0x_get(22), () => {
      navigator.clipboard.writeText(DUID).then(() => {
        const toast = fkk(_0x_get(135));
        if (toast) {
          toast.textContent = `Copied Discord ID: ${DUID}`;
          toast.classList.remove(_0x_get(53));
          setTimeout(() => toast.classList.add(_0x_get(53)), 2200);
        }
      });
    });
  }
}


function init_timezone() {
  const fkk = x => document.getElementById(x);

  function nl_clock() {
    const t_el = fkk(_0x_get(136));
    const d_el = fkk(_0x_get(137));
    if (!t_el || !d_el) return;

    const now = new Date();
    const f = new Intl.DateTimeFormat(_0x_get(138), {
      timeZone: _0x_get(139),
      hour: _0x_get(140),
      minute: _0x_get(140),
      second: _0x_get(140),
      hour12: false
    });
    t_el.textContent = `${f.format(now)} CET`;

    try {
      const nl_str = now.toLocaleString(_0x_get(141), { timeZone: _0x_get(139) });
      const loc_str = now.toLocaleString(_0x_get(141));
      const nl_ms = new Date(nl_str).getTime();
      const loc_ms = new Date(loc_str).getTime();
      const diff_h = Math.round((nl_ms - loc_ms) / (1000 * 60 * 60));

      if (diff_h === 0) {
        d_el.textContent = _0x_get(142);
      } else if (diff_h > 0) {
        d_el.textContent = `(+${diff_h}h ahead)`;
      } else {
        d_el.textContent = `(${diff_h}h behind)`;
      }
    } catch (_) {
      d_el.textContent = '';
    }
  }

  nl_clock();
  setInterval(nl_clock, 1000);
}


function init_timer() {
  const fkk = x => document.getElementById(x);

  function timer_tick() {
    const tgt = new Date(_0x_get(143)).getTime();
    const diff = Math.max(0, tgt - Date.now());

    const d = Math.floor(diff / (1000 * 60 * 60 * 24));
    const h = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const m = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
    const s = Math.floor((diff % (1000 * 60)) / 1000);

    const elD = fkk(_0x_get(144));
    const elH = fkk(_0x_get(145));
    const elM = fkk(_0x_get(146));
    const elS = fkk(_0x_get(147));

    if (elD) elD.textContent = d;
    if (elH) elH.textContent = h.toString().padStart(2, '0');
    if (elM) elM.textContent = m.toString().padStart(2, '0');
    if (elS) elS.textContent = s.toString().padStart(2, '0');
  }

  timer_tick();
  setInterval(timer_tick, 1000);
}


function init_qr() {
  const fkk = x => document.getElementById(x);

  function pop_toast(msg) {
    const t = fkk(_0x_get(135));
    if (!t) return;
    const txt = fkk(_0x_get(148));
    if (txt) txt.textContent = msg;
    else t.textContent = msg;
    t.classList.remove(_0x_get(53));
    clearTimeout(t._timer);
    t._timer = setTimeout(() => t.classList.add(_0x_get(53)), 2200);
  }

  function copy_text(val) {
    if (!val) return;
    try {
      const el = document.createElement(_0x_get(149));
      el.value = val;
      el.setAttribute(_0x_get(150), '');
      el.style.position = _0x_get(151);
      el.style.left = _0x_get(152);
      el.style.top = '0';
      document.body.appendChild(el);
      el.select();
      el.setSelectionRange(0, 99999);
      document.execCommand(_0x_get(153));
      document.body.removeChild(el);
    } catch (e) {}

    if (navigator.clipboard && navigator.clipboard.writeText) {
      navigator.clipboard.writeText(val).catch(() => {});
    }

    pop_toast(_0x_get(154));
  }

  function gen_qr(str) {
    const sz = 21;
    let grid = Array(sz).fill(0).map(() => Array(sz).fill(false));

    function box(r, c) {
      for (let i = 0; i < 7; i++) {
        for (let j = 0; j < 7; j++) {
          if (i === 0 || i === 6 || j === 0 || j === 6 || (i >= 2 && i <= 4 && j >= 2 && j <= 4)) {
            grid[r + i][c + j] = true;
          }
        }
      }
    }
    box(0, 0);
    box(0, sz - 7);
    box(sz - 7, 0);

    for (let i = 8; i < sz - 8; i++) {
      grid[6][i] = i % 2 === 0;
      grid[i][6] = i % 2 === 0;
    }

    let h = 0;
    for (let i = 0; i < str.length; i++) {
      h = (h << 5) - h + str.charCodeAt(i);
      h |= 0;
    }

    for (let r = 0; r < sz; r++) {
      for (let c = 0; c < sz; c++) {
        if ((r < 8 && c < 8) || (r < 8 && c >= sz - 8) || (r >= sz - 8 && c < 8)) continue;
        if (r === 6 || c === 6) continue;
        const v = Math.abs(Math.sin(h + r * 13 + c * 37));
        grid[r][c] = v > 0.48;
      }
    }

    const cell = 7;
    const w = sz * cell;
    let svg = `<svg viewBox=_0x_get(155) xmlns=_0x_get(156)>`;
    svg += `<rect width=_0x_get(157) height=_0x_get(157) fill=_0x_get(158)/>`;
    for (let r = 0; r < sz; r++) {
      for (let c = 0; c < sz; c++) {
        if (grid[r][c]) {
          svg += `<rect x=_0x_get(159) y=_0x_get(160) width=_0x_get(161) height=_0x_get(161) fill=_0x_get(13)/>`;
        }
      }
    }
    svg += `</svg>`;
    return svg;
  }

  document.querySelectorAll(_0x_get(162)).forEach(b => {
    b.addEventListener(_0x_get(22), (e) => {
      e.preventDefault();
      const tid = b.getAttribute(_0x_get(163));
      let val = b.getAttribute(_0x_get(164));
      if (!val && tid) {
        const inp = document.getElementById(tid);
        if (inp) val = inp.value;
      }
      if (!val) {
        const p = b.closest(_0x_get(165));
        if (p) {
          const inp = p.querySelector(_0x_get(65));
          if (inp) val = inp.value;
        }
      }
      copy_text(val);
    });
  });

  const qrm = fkk(_0x_get(166));
  document.querySelectorAll(_0x_get(167)).forEach(b => {
    b.addEventListener(_0x_get(22), (e) => {
      e.preventDefault();
      const coin = b.getAttribute(_0x_get(168)) || _0x_get(169);
      const addr = b.getAttribute(_0x_get(164)) || '';
      fkk(_0x_get(170)).textContent = `${coin} QR`;
      fkk(_0x_get(171)).textContent = addr;
      fkk(_0x_get(172)).innerHTML = gen_qr(addr);
      if (qrm) qrm.classList.remove(_0x_get(53));
    });
  });

  if (qrm) {
    const mc = fkk(_0x_get(173));
    if (mc) mc.addEventListener(_0x_get(22), () => qrm.classList.add(_0x_get(53)));
    qrm.addEventListener(_0x_get(22), e => {
      if (e.target === qrm) qrm.classList.add(_0x_get(53));
    });
    const mcp = fkk(_0x_get(174));
    if (mcp) {
      mcp.addEventListener(_0x_get(22), () => {
        const addr = fkk(_0x_get(171)).textContent;
        copy_text(addr);
        qrm.classList.add(_0x_get(53));
      });
    }
  }

  document.querySelectorAll(_0x_get(175)).forEach(c => {
    c.addEventListener(_0x_get(18), e => {
      const r = c.getBoundingClientRect();
      const x = e.clientX - r.left - r.width / 2;
      const y = e.clientY - r.top - r.height / 2;
      const rx = -(y / (r.height / 2)) * 4;
      const ry = (x / (r.width / 2)) * 4;
      c.style.transform = `perspective(800px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-4px)`;
    });
    c.addEventListener(_0x_get(19), () => {
      c.style.transform = _0x_get(176);
    });
  });
}


function init_nav() {
  const bar = document.getElementById(_0x_get(177));
  if (!bar) return;
  const bubble = document.getElementById(_0x_get(178));
  const links = bar.querySelectorAll(_0x_get(179));
  if (!links.length || !bubble) return;

  function set_bubble(el) {
    links.forEach(l => l.classList.remove(_0x_get(58)));
    el.classList.add(_0x_get(58));

    const barRect = bar.getBoundingClientRect();
    const elRect = el.getBoundingClientRect();
    const x = elRect.left - barRect.left;
    const w = elRect.width;

    bubble.style.width = `${w}px`;
    bubble.style.transform = `translateX(${x}px)`;
    bubble.style.opacity = '1';
  }

  links.forEach(l => {
    l.addEventListener(_0x_get(180), () => set_bubble(l));
    l.addEventListener(_0x_get(22), (e) => {
      const href = l.getAttribute(_0x_get(181));
      if (href && href.startsWith('#')) {
        e.preventDefault();
        set_bubble(l);
        const target = document.querySelector(href);
        if (target) {
          const y = target.getBoundingClientRect().top + window.pageYOffset - 110;
          window.scrollTo({ top: y, behavior: _0x_get(182) });
        }
      }
    });
  });

  bar.addEventListener(_0x_get(19), () => {
    update_scrollspy();
  });

  function update_scrollspy() {
    const scrollPos = window.scrollY + 200;
    const s_serv = document.getElementById(_0x_get(183));
    const s_proj = document.getElementById(_0x_get(184));
    const s_cryp = document.getElementById(_0x_get(185));

    let current = links[0];
    if (s_cryp && scrollPos >= s_cryp.offsetTop) {
      current = bar.querySelector(_0x_get(186)) || current;
    } else if (s_proj && scrollPos >= s_proj.offsetTop) {
      current = bar.querySelector(_0x_get(187)) || current;
    } else if (s_serv && scrollPos >= s_serv.offsetTop) {
      current = bar.querySelector(_0x_get(188)) || current;
    }
    set_bubble(current);
  }

  const navWrap = document.querySelector(_0x_get(189));
  function check_dock_scroll() {
    if (navWrap) {
      if (window.scrollY > 30) {
        navWrap.classList.add(_0x_get(190));
      } else {
        navWrap.classList.remove(_0x_get(190));
      }
    }
  }

  window.addEventListener(_0x_get(21), () => {
    update_scrollspy();
    check_dock_scroll();
  }, { passive: true });

  setTimeout(() => {
    update_scrollspy();
    check_dock_scroll();
  }, 150);

  window.addEventListener(_0x_get(17), () => {
    const cur = bar.querySelector(_0x_get(191)) || links[0];
    if (cur) set_bubble(cur);
  });
}


function init_fx() {
  const statExp = document.getElementById(_0x_get(192));
  const statSafe = document.getElementById(_0x_get(193));
  const statYear = document.getElementById(_0x_get(194));

  function animate_num(el, start, target, suffix, duration, delay) {
    if (!el) return;
    setTimeout(() => {
      const startTime = performance.now();
      function update(currentTime) {
        const elapsed = currentTime - startTime;
        const progress = Math.min(1, elapsed / duration);
        const ease = 1 - Math.pow(1 - progress, 3);
        const current = Math.floor(start + (target - start) * ease);
        el.textContent = current + suffix;
        if (progress < 1) {
          requestAnimationFrame(update);
        } else {
          el.textContent = target + suffix;
        }
      }
      requestAnimationFrame(update);
    }, delay);
  }

  animate_num(statExp, 0, 2, _0x_get(195), 1100, 300);
  animate_num(statSafe, 0, 100, '%', 1400, 450);
  animate_num(statYear, 2020, 2027, '', 1600, 600);

  const tiltCards = document.querySelectorAll(_0x_get(175));
  tiltCards.forEach(card => {
    card.addEventListener(_0x_get(18), e => {
      const rect = card.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      const centerX = rect.width / 2;
      const centerY = rect.height / 2;
      const rotateX = ((y - centerY) / centerY) * -6;
      const rotateY = ((x - centerX) / centerX) * 6;
      const shineX = (x / rect.width) * 100;
      const shineY = (y / rect.height) * 100;

      card.style.transform = `perspective(800px) rotateX(${rotateX.toFixed(2)}deg) rotateY(${rotateY.toFixed(2)}deg) translateY(-2px)`;
      card.style.setProperty(_0x_get(196), `${shineX.toFixed(1)}%`);
      card.style.setProperty(_0x_get(197), `${shineY.toFixed(1)}%`);
    });

    card.addEventListener(_0x_get(19), () => {
      card.style.transform = _0x_get(198);
      card.style.setProperty(_0x_get(196), _0x_get(199));
      card.style.setProperty(_0x_get(197), _0x_get(199));
    });
  });

  let lastY = window.scrollY;
  let vel = 0;
  let dVel = 0;
  window.addEventListener(_0x_get(21), () => {
    const y = window.scrollY;
    vel = y - lastY;
    lastY = y;
  }, { passive: true });

  const bCards = document.querySelectorAll(_0x_get(200));
  function bLoop() {
    dVel += (vel - dVel) * 0.12;
    vel *= 0.86;
    const str = Math.max(-0.025, Math.min(0.025, dVel * 0.0008));
    if (Math.abs(str) > 0.0003) {
      bCards.forEach(c => {
        if (!c.hasAttribute(_0x_get(201)) || !c.matches(_0x_get(202))) {
          c.style.transform = `scale(${1 - str * 0.4}, ${1 + str}) translateY(${-dVel * 0.06}px)`;
        }
      });
    } else {
      bCards.forEach(c => {
        if (!c.hasAttribute(_0x_get(201)) && c.style.transform) {
          c.style.transform = '';
        }
      });
    }
    requestAnimationFrame(bLoop);
  }
  bLoop();

  const io = new IntersectionObserver(ents => {
    ents.forEach(e => {
      if (e.isIntersecting) e.target.classList.add(_0x_get(203));
    });
  }, { threshold: 0.12 });
  document.querySelectorAll(_0x_get(204)).forEach(el => {
    el.classList.add(_0x_get(205));
    io.observe(el);
  });
}


function init_views() {
  const el = document.getElementById(_0x_get(206));
  if (!el) return;

  const tick = (from, to, ms) => {
    const start = performance.now();
    const step = () => {
      const p = Math.min((performance.now() - start) / ms, 1);
      const v = Math.round(from + (to - from) * (1 - Math.pow(1 - p, 3)));
      el.textContent = v.toLocaleString();
      if (p < 1) requestAnimationFrame(step);
    };
    requestAnimationFrame(step);
  };

  fetch(_0x_get(207))
    .then(r => r.ok ? r.json() : null)
    .then(d => {
      if (!d) return;
      tick(0, d.views, 1200);
    })
    .catch(() => {});
}




document.addEventListener(_0x_get(208), () => {
  init_canvas();
  init_audio();
  init_discord();
  init_timezone();
  init_timer();
  init_qr();
  init_nav();
  init_fx();
  init_views();


  const b_btn = document.getElementById(_0x_get(209));
  const m_drw = document.getElementById(_0x_get(210));
  if (b_btn && m_drw) {
    b_btn.addEventListener(_0x_get(22), () => {
      m_drw.classList.toggle(_0x_get(53));
      b_btn.classList.toggle(_0x_get(211));
    });
    document.querySelectorAll(_0x_get(212)).forEach(l => {
      l.addEventListener(_0x_get(22), () => {
        m_drw.classList.add(_0x_get(53));
        b_btn.classList.remove(_0x_get(211));
      });
    });
  }
});


});